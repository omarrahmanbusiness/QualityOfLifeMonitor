//
//  Configuration.swift
//  QualityOfLifeMonitor
//
//  Copy this file to Configuration.swift and fill in your credentials.
//  Configuration.swift is gitignored and won't be committed.
//

import Foundation

enum Config {
    // Supabase Configuration
    static let supabaseURL = "YOUR_SUPABASE_URL"  // e.g., "https://xxxxx.supabase.co"
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"

    // Email redirect URL (for email confirmation)
    // Set this to your app's URL scheme or a web URL that handles the redirect
    // e.g., "qualityoflifemonitor://auth/callback" or "https://yourapp.com/auth/callback"
    static let emailRedirectURL = "YOUR_REDIRECT_URL"
}
